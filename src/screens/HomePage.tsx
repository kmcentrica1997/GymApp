import React, {FC} from 'react';
import {View, StyleSheet} from 'react-native';

const Home: FC = props => {
  return <View></View>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
export default Home;
